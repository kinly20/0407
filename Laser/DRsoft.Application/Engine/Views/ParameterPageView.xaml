<UserControl
    x:Class="Engine.Views.ParameterPageView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.5*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="0.1*" />
        </Grid.RowDefinitions>

        <!--<Viewbox
            Grid.Row="0"
            Grid.Column="0"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Stretch="Uniform">
            <ContentControl x:Name="ParameterRecipeViewContent" />
        </Viewbox>-->

        <Viewbox
            Grid.Row="0"
            Grid.RowSpan="2"
            Grid.Column="0"
            Grid.ColumnSpan="2"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Stretch="Uniform">
            <ContentControl x:Name="ParameterDetailViewContent" />
        </Viewbox>

        <!--<ContentControl
            x:Name="DataLogLargeViewContent"
            Grid.Row="1"
            Grid.Column="0"
            Grid.ColumnSpan="2" />-->
    </Grid>
</UserControl>
